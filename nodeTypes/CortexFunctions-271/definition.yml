fileVersion: 1
id: "271"
isDisabled: false
metadata:
  defaultStorageLocation: null
  error: null
  nodeMetadataSpec: |-
    capitalized: Cortex Functions 
    short: LLM
    plural: llms
    tagColor: '#a332f0'

    config:
    - groupName: Options
      items:
      - type: materializationSelector
        default: table
        options:
        - table
        isRequired: true

      - type: multisourceToggle
        enableIf: "{% if node.materializationType == 'table' %} true {% else %} false {% endif %}" 

      - type: overrideSQLToggle
        enableIf: "{% if node.materializationType == 'view' %} true {% else %} false {% endif %}"

      - displayName: Multi Source Strategy
        attributeName: insertStrategy
        type: dropdownSelector
        default: INSERT
        options:
        - "INSERT"
        - "UNION"
        - "UNION ALL"
        isRequired: true
        enableIf: "{% if node.isMultisource %} true {% else %} false {% endif %}"

      - displayName: Truncate Before
        attributeName: truncateBefore
        type: toggleButton
        default: true

      - displayName: Enable Tests
        attributeName: testsEnabled
        type: toggleButton
        default: true

      - displayName: Pre-SQL
        attributeName: preSQL
        type: textBox
        syntax: sql
        isRequired: false

      - displayName: Post-SQL
        attributeName: postSQL
        type: textBox
        syntax: sql
        isRequired: false
    - groupName: Cortex Package
      items:
      - displayName: Use Legacy Functions
        attributeName: useLegacyFunctions
        type: toggleButton
        default: false

      - displayName: SUMMARIZE
        attributeName: summarizefunction
        type: toggleButton
        default: false

      - displayName: 'Select Columns '
        type: tabular
        attributeName: pby
        columns:
        - type: columnDropdownSelector
          displayName: Column Name
          attributeName: partColName
          isRequired: true
        isRequired: false
        enableIf: '{% if config.summarizefunction %} true {% else %} false {% endif %}'

      - displayName: SENTIMENT
        attributeName: sentimentfunction
        type: toggleButton
        default: false
        enableIf: '{% if config.useLegacyFunctions %} true {% else %} false {% endif %}'

      - displayName: AI_SENTIMENT
        attributeName: aiSentimentFunction
        type: toggleButton
        default: false
        enableIf: '{% if not config.useLegacyFunctions %} true {% else %} false {% endif %}'

      - displayName: 'Select Columns '
        type: tabular
        attributeName: sby
        columns:
        - type: columnDropdownSelector
          displayName: Column Name
          attributeName: spartColName
          isRequired: true
        isRequired: false
        enableIf: '{% if config.sentimentfunction or config.aiSentimentFunction %} true {% else %} false {% endif %}'

      - displayName: TRANSLATE
        attributeName: TranslateFunction
        type: toggleButton
        default: false
        enableIf: '{% if config.useLegacyFunctions %} true {% else %} false {% endif %}'

      - displayName: AI_TRANSLATE
        attributeName: aiTranslateFunction
        type: toggleButton
        default: false
        enableIf: '{% if not config.useLegacyFunctions %} true {% else %} false {% endif %}'

      - type: tabular
        displayName: 'Select Columns'
        attributeName: tby
        columns:
          - type: columnDropdownSelector
            displayName: Column Name
            attributeName: tpartColName
            isRequired: true

          - type: textBox
            displayName: Translate from
            attributeName: lanorder
            default: "'From', 'To'"
            isRequired: true

        isRequired: false
        enableIf: '{% if config.translatefunction or config.aiTranslateFunction %} true {% else %} false {% endif %}'

      - displayName: EXTRACT ANSWER
        attributeName: extractfunction
        type: toggleButton
        default: false
        enableIf: '{% if config.useLegacyFunctions %} true {% else %} false {% endif %}'

      - displayName: AI_EXTRACT ANSWER
        attributeName: aiExtractFunction
        type: toggleButton
        default: false
        enableIf: '{% if not config.useLegacyFunctions %} true {% else %} false {% endif %}'

      - displayName: Datatype for target column must be an ARRAY.
        attributeName: extractArrayInfo
        type: label
        enableIf: '{%- if config.useLegacyFunctions %}true{%- else %}false{%- endif %}'
    	
      - displayName: Datatype for target column must be an VARIANT.
        attributeName: extractVariantInfo
        type: label
        enableIf: '{%- if not config.useLegacyFunctions %}true{%- else %}false{%- endif %}'

      - type: tabular
        displayName: 'Select Columns'
        attributeName: exby
        columns:
          - type: columnDropdownSelector
            displayName: Column Name
            attributeName: epartColName
            isRequired: true

          - type: textBox
            displayName: Ask me a Question
            attributeName: askque
            default: "what''s the Question ?"
            isRequired: true

        isRequired: false
        enableIf: '{% if config.extractfunction or config.aiExtractFunction %} true {% else %} false {% endif %}'

      - displayName: AI_COMPLETE
        attributeName: aiCompleteFunction
        type: toggleButton
        default: false
        enableIf: '{% if not config.useLegacyFunctions %} true {% else %} false {% endif %}'

      - type: tabular
        displayName: 'AI Complete Configuration'
        attributeName: cby
        columns:
          - type: columnDropdownSelector
            displayName: Column Name
            attributeName: cpartColName
            isRequired: true

          - type: dropdownSelector
            displayName: AI Engine
            attributeName: modelName
            default: claude-4-sonnet
            options:
            - claude-4-sonnet
            - claude-4-opus
            - mistral-large2
            isRequired: true

          - type: textBox
            displayName: Ask me a Question
            attributeName: promptText
            default: 'Summarize this text'
            isRequired: true

        isRequired: false
        enableIf: '{% if config.aiCompleteFunction %} true {% else %} false {% endif %}'
    	
      - displayName: AI_CLASSIFY
        attributeName: aiClassifyFunction
        type: toggleButton
        default: false
        enableIf: '{% if not config.useLegacyFunctions %} true {% else %} false {% endif %}'
    	
      - type: tabular
        displayName: 'AI Classify Configuration'
        attributeName: clby
        columns:
          - type: columnDropdownSelector
            displayName: Column Name
            attributeName: classColName
            isRequired: true

          - type: textBox
            displayName: Categories (comma separated)
            attributeName: categories
            default: 'Positive,Negative,Neutral'
            isRequired: true

        default:
          - categories: 'Positive,Negative,Neutral'

        isRequired: false
        enableIf: '{% if config.aiClassifyFunction %} true {% else %} false {% endif %}'
name: Cortex Functions
type: NodeType
